# living-threat-intel requirements
# Python 3.11+

# HTTP — core dependency for all collectors
requests==2.32.3
urllib3==2.2.3

# YAML — storage format
PyYAML==6.0.2

# Anthropic SDK — weekly summarizer
anthropic==0.40.0

# Progress and logging
rich==13.9.4         # optional: pretty terminal output for local runs

# Dev / testing only (not needed in CI)
pytest==8.3.4
pytest-mock==3.14.0
responses==0.25.3    # mock HTTP for collector tests
